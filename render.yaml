services:
  - type: web
    name: idari-dava-dilekce-kontrol
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py --host 0.0.0.0
    autoDeploy: true
